export class Vehicle {
  color: string;
  height: number;
  weight: number;
  length: number;

  constructor(color: string, height: number, weight: number, length: number) {
    this.color = color;
    this.height = height;
    this.weight = weight;
    this.length = length;
  }
}
