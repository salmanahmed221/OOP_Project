export class Vehicle {
    color;
    height;
    weight;
    length;
    constructor(color, height, weight, length) {
        this.color = color;
        this.height = height;
        this.weight = weight;
        this.length = length;
    }
}
